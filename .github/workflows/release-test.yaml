name: Release Frontend to Test
on:
  workflow_dispatch:

jobs:
  build:
    name: Build-PokeOneWebApp
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: PokeOneWeb/Frontend/PokeOneWeb/package-lock.json

      - run: |
          npm ci
          npm version $GITHUB_RUN_NUMBER.$GITHUB_RUN_ATTEMPT.$(date +'%Y-%m-%d')
          npm run build -- --configuration test
        working-directory: PokeOneWeb/Frontend/PokeOneWeb

      - uses: appleboy/scp-action@master
        with:
          host: test.pokeone.guide
          username: ubuntu
          port: 22
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_KEY_PASSPHRASE }}
          source: "PokeOneWeb/Frontend/PokeOneWeb/dist"
          target: "/opt/PokeOneWeb.WebApp/test"
          strip_components: 5
          overwrite: true
